# SVM-Navie-bayes
Support Vector Machine (SVM) is a supervised learning algorithm used for classification and regression. It finds the optimal hyperplane that separates data points of different classes with maximum margin. Naïve Bayes is a probabilistic algorithm based on Bayes’ theorem, assuming features are independent.
